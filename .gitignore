# มี LINE_CHANNEL_SECRET จริงอยู่ในไฟล์นี้ — ห้าม commit ขึ้น GitHub เด็ดขาด
# ยังคง clasp push ขึ้น Apps Script ได้ตามปกติ (คนละช่องทางกับ git)
backend/script_properties.gs

node_modules/
.DS_Store

# SheetJS สำหรับทดสอบตัวอ่านไฟล์ใน node เท่านั้น (ดาวน์โหลดจาก CDN) — ห้าม commit
.dev/xlsx.full.min.js
.dev_scratch/

# ใบราคา/ข้อมูลราคาจริงของบริษัท — เก็บบนไดรฟ์ ห้ามขึ้น GitHub
reference/
